if you want to run from docker compose... 
you should be in this directory and run the command : docker-compose up --build

docker compose file is building the images from dockerFile in each service (vite / express) 